plugin: aws_ec2

regions:
  - ap-southeast-2  # specify your AWS region


keyed_groups:
  - key: tags.Name
    prefix: tag_Name
  - key: placement.region
    prefix: aws_region
  - key: tags.Project
    prefix: tag_Project

